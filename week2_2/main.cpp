#include <string>
#include <vector>
#include <queue>

using namespace std;

int solution(vector<int> priorities, int location) {
	int answer = 0;
	queue<int> que; 

	for (auto elem : priorities)
	{
		que.push(elem);
	}

	return answer;
}

void main()
{

}